class Solution {
    public boolean isPalindrome(int x) {
        int r = 0, t = x;
        while (t > 0) {
            r = (r * 10) + (t % 10);
            t /= 10;
        }
        if (r == x)
        return true;
        else
        return false;
    }
}
